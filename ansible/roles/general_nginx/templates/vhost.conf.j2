server {
    listen  80 default;
    root /var/www/;
    location / {
        autoindex off;
    }

    location ~ ^/(?P<service>{{ groups['services'] | join('|') }})$ {
        rewrite ^ http://$service.$host redirect;
    }
}
